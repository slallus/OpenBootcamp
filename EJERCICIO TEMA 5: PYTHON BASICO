# Escribe una función que pueda decirte si un año (número entero) es bisiesto o no.

año = float(input("Que año es: "))

if año % 4 == 0 and (año % 100 != 0 or año % 400 == 0):
	print("Es bisiesto")
else:
	print("No es bisiesto")
